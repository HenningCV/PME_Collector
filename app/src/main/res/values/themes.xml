<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Collector" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- primary color -->
        <item name="colorPrimary">@color/main_yellow</item>
        <item name="colorPrimaryVariant">@color/secondary_yellow</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- secondary color -->
        <item name="colorSecondary">@color/background_item_brown</item>
        <item name="colorSecondaryVariant">@color/background_black</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
    </style>
</resources>